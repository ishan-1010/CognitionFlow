# CognitionFlow environment (copy to .env and fill in)
# Use either Groq or OpenAI.

# Groq (recommended for low latency)
GROQ_API_KEY=your_groq_key_here
# Optional: GROQ_MODEL=openai/gpt-oss-120b
# Optional: GROQ_TEMPERATURE=0.1
# Optional: GROQ_TIMEOUT=600

# Or OpenAI
# OPENAI_API_KEY=your_openai_key_here
# Optional: OPENAI_MODEL=gpt-4o
# Optional: OPENAI_BASE_URL=
# Optional: OPENAI_TEMPERATURE=0.1
# Optional: OPENAI_TIMEOUT=600

# Where the agent runs code and writes artifacts (default: project_workspace)
# COGNITIONFLOW_WORKSPACE=project_workspace
